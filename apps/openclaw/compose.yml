services:
  ssh:
    build: .
    container_name: openclaw-ssh
    restart: unless-stopped
    ports:
      - "2222:22"
      - "127.0.0.1:18789:18789"
    env_file: .env
    volumes:
      - ./data/home:/home/openclaw
      - ./authorized_keys:/tmp/authorized_keys:ro
